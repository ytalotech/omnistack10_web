import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// aqui ele esta mandando nosso código do app para dentro do root
ReactDOM.render(<App />, document.getElementById('root'));

//JSX (Javascript + html)